# claude-code-bridge

A thin MCP server that lets a sandboxed agent (Cowork, Cursor, etc.) delegate host-side operations to Claude Code running on the user's machine.

Status: pre-alpha (Phase 1 PoC). See `USE-CLAUDE-CODE-TOOL-PLAN-2026-05-13.md` in `C:\dev\Teaching` for the design.
